

export default function ControlsHelpModal() {
  return (
    <div>
      f10 (or whatever it is) for focusing messages on the top of the screen
      Ctrl + q is for focusing the right panels dragger button then use the left or right arrow keys to expand left or right.
    </div>
  )
}
